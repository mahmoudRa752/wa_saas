# /helpers

Small, stateless, reusable utility functions that don't belong to a single
Service or Repository (e.g. formatting, string/array utilities, validation
helpers shared across multiple Services). Not a dumping ground for business
logic — if it needs to know about tenants, roles, or the database, it
belongs in a Service or Repository instead.

Empty in this delivery.
